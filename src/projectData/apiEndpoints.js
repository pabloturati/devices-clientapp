const baseURL = 'http://localhost:3000'

const apiEndpoints = {
  getDevices: `${baseURL}/devices`,
  postDevice: `${baseURL}/devices`,
  deleteDevice: `${baseURL}/devices`
}

export default apiEndpoints
